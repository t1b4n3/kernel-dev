

A major number is the unique identifier to the kernel module.

The number is automatically assigned if you pass `0` to the first parameter of `register_chrdev`, but you can specify it.


The file_operations struct is 

[file_oprations](https://elixir.bootlin.com/linux/v6.12.3/source/include/linux/fs.h#L2062)

if custom kernel them compile io.c with static option
